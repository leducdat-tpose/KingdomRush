using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThugAttackState : BaseState<DesertThug>
{
    public ThugAttackState(DesertThug obj, StateManager<DesertThug> objectStateManager) : base(obj, objectStateManager)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
